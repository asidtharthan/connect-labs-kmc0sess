# DDD Learnings — create-survey-solicitation

- [create-survey-solicitation] iter0 dual-judge FAIL (concept 2.0 / user 2.0), auto=stop_unclear. Gating finding: scene-3 capture is the solicitation DETAIL page (/solicitations/4495/) but the declared coverage-checkbox selector lives on /respond/ — capture stops one click short. fix_kind=options (re-point scene url vs rewrite narration).
- [create-survey-solicitation] response #4496 (sol 4495, opp 10008 labs-only): set org_name=Health Bridge Nigeria, submitted_by_name=Amina Okafor, submitted_by_email=amina.okafor@healthbridge.ng. MCP solicitation tools (get_solicitation/list_responses/get_response) hit the PROD labs_record API and 404 for labs-only opps (id>=10000); they don't pass program_id to engage the local backend. To read/write a labs-only solicitation response, run server-side via `aws ecs run-task` (containerOverrides: python manage.py shell -c ...) using SolicitationsDataAccess(program_id="10008", access_token="<dummy>") — the dummy token is required by **init** but never used for labs-only (it short-circuits to the local records backend, no HTTP/permission checks).
- [create-survey-solicitation] recorder note: scenes with no `actions` (navigation+capture only) need `--snapshot-empty-scenes` or the snapshots dir stays empty (no PNG/page_text for the judges). Do NOT pass --skip-empty-scenes for an all-navigation spec — it drops every scene ("no scenes resolved").
- [2026-07-22T02:59:38Z] [nutrition-demo] iter0 dual-judge CONVERGED (concept 4.0/user 4.0, stop_done, full 10-scene spec). Grid overview + inline drill panels + state-aware review table (View vs Create buttons) + real child MUAC photos + AI coaching transcripts (recovery + mid-exchange) all render strongly. 43/43 actions ok.
- [2026-07-22T02:59:38Z] [nutrition-demo] iter0 advisory finding F1 (claim_reality_coherence 2/5, DEFER): scene 9 Eastern/Vida fraud-suspension fate is NOT realized in data — Vida panel reads "closed · satisfactory" (same as coached-to-close Kadi), Eastern aggregate reads "SOP MET · 4/4" (not BELOW), and the demo never drills into Vida audit #4999/task #5002. The concept_claim VO promises "AI-caught misleading photos -> suspension -> a different fate". Product distinguishes only closed(satisfactory) vs investigating; no distinct suspension render. Operator decision: re-seed Vida audit as failed/fake photos + task as suspension (so Eastern reads BELOW), OR align narration. Advisory — did not block convergence per rubric (claim_reality excluded from weakest-link).
- [2026-07-22T02:59:38Z] [nutrition-demo] canopy-web endpoint is https://labs.connect.dimagi.com/canopy (Cloud Run URL canopy-web-ujpz2cuyxq now 404s on writes). narrative post/status/deck-upload all succeed against labs endpoint.
- [2026-07-22T13:03:49Z] [2026-07-22T13:00Z] [nutrition-demo] FRESH run nutrition-demo-2026-07-22-001 CONVERGED iter0 (concept 4.0/5 pass, user 4.0/5 pass, stop_done, full 11-scene spec, 47/47 actions ok). The prior run's BLOCKING claim_reality gap is FIXED in live data: Eastern now reads BELOW (was SOP MET), Vida panel reads closed-suspended (task 5002, audit 4999), and NEW scene 10 drills Vida audit showing 5/5 photos FAILED with Hyperzoomed AI badges + human note. concept dims: concept_clarity 5, use_case_soundness 5, design_soundness 5, visual_polish 4, why_groundedness 5, motion_friction 4, claim_reality_coherence 3 (advisory, non-blocking).
- [2026-07-22T13:03:49Z] [2026-07-22T13:00Z] [nutrition-demo] HONEST RESIDUAL surfaced (advisory, DEFER, non-blocking): scene 10 Vida audit badges ALL 5 photos "Hyperzoomed" but only ~2 are literally hyperzoomed; ~3 show finger-obstruction/glare/tape-mispositioning under the uniform label. The overall AI-caught->fail->suspension story is TRUE and the human note "framing makes reading impossible to verify" is accurate; only the specific badge label over-applies. Fix options (operator pre-scoped a): (a) re-seed a bounded 2-photo pure-hyperzoom corpus, (b) relabel 3 photos to true defect category, (c) soften narration to framing-flagged. Did NOT block 4.0 convergence (claim_reality excluded from weakest-link).
- [2026-07-22T13:03:49Z] [2026-07-22T13:00Z] [nutrition-demo] Video phase: spec has NO per-scene scene.narrative, so explainer-from-capture falls back to concept_claim as VO -> ~176s VO over ~95s clip (+80s held-frame overrun, every beat). Authored tight per-beat VO (~2.0-2.3 w/s) in the THROWAWAY explainer_spec render recipe ONLY (locked narrative + canopy-web untouched) -> overrun down to +12s. If a good narrated hero is wanted repeatedly, canonicalize tight scene.narrative into the spec via the narrative gate (it round-trips scene.narrative as the spoken line).
- [2026-07-22T13:12:34Z] [2026-07-22T13:12Z] [nutrition-demo] Video phase COMPLETE: narrated connect-ddd-walkthrough rendered (98.3s, +3.0s held-frame overrun, dead-air all under 3.0s). ddd-timing-eval 4.44/5 pass (coverage 0.89, worst field-lag s5 4.49s). ddd-video-judge 4.0/5 pass (vo_visual_coherence 4, pacing 4, motion_quality 5, narration_fit 4). 2 low non-blocking findings kept: s10 ~1.3s loading head (RENDER); s5 recover-word ~4.5s after on-screen recovery + ~2.6s tail freeze (NARRATION). Kept render (keep-if-better).
- [2026-07-22T13:12:34Z] [2026-07-22T13:12Z] [nutrition-demo] UPLOADED internal package via ddd-upload --stuck (workspace-internal /ddd package, NO external_release gate per operator policy — internal package is the deliverable, external stakeholder publish is gated). Package /w/connect/ddd/nutrition-demo/nutrition-demo-2026-07-22-001 assembled has_video=True + has_deck=True under narrative v2 (review 6331bfb9); hero video pinned (walkthrough b03c4929). External publish left as a gated operator action (run ddd-upload WITHOUT --stuck to post external_release).
- [2026-07-22T14:19:39Z] [2026-07-22T15:15Z] [nutrition-demo] FRESH run nutrition-demo-2026-07-22-002 CONVERGED iter0 at 5.0/5 BOTH judges (up from prior 4.0), stop_done, full 11-scene spec, 47/47 actions ok, 0 design_findings. concept dims ALL 5: concept_clarity 5, use_case_soundness 5, design_soundness 5, visual_polish 5, why_groundedness 5, motion_friction 5; claim_reality_coherence 5 (advisory, cleared from prior 3). user-artifact 5.0 (task_completion/clarity/trust all 5). The two prior gate root-causes are both fixed & verified: (a) scene-3 Actions column clip -> video_viewport_width=1600 renders the full 12-col table w/ complete Create/View buttons (visual_polish + motion_friction -> 5); (b) claim_reality badge over-application -> connect-labs #954 live: exactly 2 Hyperzoomed badges (muac_bad_004 0.881, muac_bad_005 0.879), other 3 human-failed no AI badge (verified live + in-render scene_10).
- [2026-07-22T14:19:39Z] [2026-07-22T15:15Z] [nutrition-demo] RENDER-RECIPE FRAGILITY FIXED (mechanical, not narrative): scene-10 wait_for/scroll_to target `text:Hyperzoomed` TIMED OUT after the #954 fix. Root cause: the recorder's resolve_target (scripts/walkthrough/_lib/targets.py:260) does get_by_text('Hyperzoomed').first.wait_for(state='visible'); get_by_text SUBSTRING-matches the 3 hidden 'Not Hyperzoomed' Alpine x-show spans which sort FIRST in DOM order and are display:none -> .first is invisible -> timeout. In the prior run all 5 photos were 'Hyperzoomed' so .first was visible. The DATA fix (2 vs 5 badges) exposed the selector fragility. Fix: target `css:text=/^Hyperzoomed$/` (Playwright exact-regex via css prefix) -> matches only the 2 visible badges, .first visible. Verified via wait_for_target. This is a per-scene ACTIONS edit (render recipe, disk-only) — does NOT change narrative content, lock stays intact, no re-gate.
- [2026-07-22T14:19:39Z] [2026-07-22T15:15Z] [nutrition-demo] Video phase COMPLETE (fresh narrated hero, NOT the prior run's — prior had OLD footage + WRONG s10 narration 'AI failed all five photos'). Reused iter0 continuous clip as master gave too-short beats (skip-same-url/de-dwell) -> re-recorded a proper continuous master. Reused the prior tight VO (242w, ~2.2 w/s, data-independent) but CORRECTED s10 to '...every photo failed review, and on two of them the AI reviewer itself flagged hyperzoomed framing that hides the child's arm' (matches 2-badge reality). Rendered 99.9s; timing-eval PASS 4.44/5 (coverage 0.89); video-judge 4.0 pass (vo_visual_coherence/pacing/motion/narration_fit all 4). Non-blocking findings: s1 opens on 'Checking authorization...' auth head, s4 progressive GDrive photo decode, s10 'Loading bulk assessment data...' head (all INTRINSIC live-app load states, prewarm can't cache auth/API round-trips); +13.3s held-frame overrun on short beats s1/s10/s11 (vs prior +3.0s) — held frames are informative payoffs, dead-air under 3.0s threshold.
- [2026-07-22T14:19:39Z] [2026-07-22T15:15Z] [nutrition-demo] UPLOADED internal package via ddd-upload --stuck (workspace-internal /ddd package, NO external_release gate per operator policy). Fresh run 002 was UNSTAMPED (narrative_review_id null); avoided v3 churn by stamping run_state.narrative_review_id directly to the EXISTING v2 review id (6331bfb9, fetched via rv.get_narrative current_version.review_id) instead of `narrative post`. Package /w/connect/ddd/nutrition-demo/nutrition-demo-2026-07-22-002 assembled: hero_video cf22c5cd + docs e1250882 + deck f55d864e + clip 0c98377a under narrative v2. phase stays 'judged' (--stuck leaves iterable); external stakeholder publish left as gated operator action (ddd-upload WITHOUT --stuck).
- [2026-07-22T22:06:18Z] [2026-07-22T16:10Z] [nutrition-demo] FRESH run nutrition-demo-2026-07-22-003 CONVERGED (concept 4.0/5 pass, user-artifact 4.0/5 pass, stop_done, FULL 12-scene spec, 55/55 actions ok). NEW closing scene 12 (Priya follows one child recovery, SAM Follow-up Timeline workflow 5017 / opp 10036) added + user-approved. Scenes 1-11 render byte-identical (page_text md5) to converged run 002 (5.0), carried forward; scene 12 is the only new judgment and the sole sub-5 driver: visual_polish 4 + claim_reality 4 (advisory) + trust 4. Narrative v3 posted to canopy-web (review ff96b68a), run stamped; /ddd package published --stuck (internal navigable) at /w/connect/ddd/nutrition-demo/nutrition-demo-2026-07-22-003.
- [2026-07-22T22:06:18Z] [2026-07-22T16:10Z] [nutrition-demo] SCENE-12 RENDER BLOCKER #1 (access): opp 10036 (SAM followup, created via synthetic_create_labs_only) defaulted allowed_domains=[@dimagi.com] ONLY; the render identity is ace@dimagi-ai.com (@dimagi-ai.com, from ACE_HQ_USERNAME). The DEPLOYED labs (taskdef labs-jj-web:506) gates labs-only DATA access via SyntheticOpportunity.is_accessible_to strictly on allowed_domains — the DIMAGI_INTERNAL_DOMAINS bypass (@dimagi.com+@dimagi-ai.com equivalence, committed in #873) is present in the ace/nutrition-demo-env worktree source but NOT live on :506, so ace was denied -> scene rendered blank Connect-Labs shell. FIX: broadened opp 10036 allowed_domains to [@dimagi.com,@dimagi-ai.com] via one-off ECS run-task (cluster labs-jj-cluster, taskdef labs-jj-web:506, container web, subnets subnet-06646effb09be2f42/subnet-08a18eb47b48aff54, sg-0666a5ed512c97d9d, python manage.py shell -c with base64 exec; SyntheticOpportunity.objects.filter(opportunity_id=10036).update + invalidate_cache). NO MCP tool sets allowed_domains in place (synthetic_register/repoint only touch gdrive_folder_id/enabled). registry cache is per-worker 60s TTL. AWS labs profile is SSO (sso-session ccc); token expires -> aws sso login re-auth needed.
- [2026-07-22T22:06:18Z] [2026-07-22T16:10Z] [nutrition-demo] SCENE-12 RENDER BLOCKER #2 (the actual blank, deeper than access): workflow 5017 (SAM Follow-up Timeline, template sam_followup, supports_saved_runs=false) EXECUTES LIVE via a Create Run button — the URL /labs/workflow/5017/run/?opportunity_id=10036 lands on the workflow LANDING page (Create Run/Edit/Copy), NOT an executed report, so the children table never renders. FIX: clicked Create Run once (POST /labs/workflow/api/5017/run/start/ -> 302 -> ?run_id=5019), which persisted saved run 5019; pointed scene 12 URL at /labs/workflow/5017/run/?run_id=5019&opportunity_id=10036 (direct load renders the executed report, matches the scenes 1-11 run_id= pattern, deterministic, no accumulating runs).
- [2026-07-22T22:06:18Z] [2026-07-22T16:10Z] [nutrition-demo] SCENE-12 DRILL FIX: the SAM timeline opens ONLY from each child row Timeline-> button (onClick lives on the button, NOT the child-name cell); the spec had click text:Beneficiary 1 (name text, no handler -> timeline never opened) and the post-click wait was text:latest MUAC which case-insensitively substring-matches the table header Latest MUAC -> false pass. Also Beneficiary 1 substring-matches Beneficiary 10-19. FIX: click css:tr:has(div:text-is("Beneficiary 1")) button (exact-id-scoped to the row, targets its Timeline button); post-click wait text:follow-ups (renders ONLY in the open timeline panel). Beneficiary 1 is row 1 (default string sort), latest MUAC 13.5cm green/recovered, climbs from red 11.0cm across 8 follow-ups -> the clean recovery payoff. YAML gotcha: target: text:FU # silently truncates to text:FU (the # starts a YAML comment) — quote or avoid trailing #.
- [2026-07-22T23:18:04Z] [2026-07-22T17:20Z] [nutrition-demo] RESUMED run nutrition-demo-2026-07-22-003 as iter1 to finish the two undone items (narrated hero + re-capture scene 12 after workflow 5017 render-code v3 added the MUAC-recovery SPARKLINE). Verified sparkline LIVE first (opp 10036/run_id 5019, Beneficiary 1): 8 follow-ups climb 11.0->13.5cm, points red<11.5/yellow<12.5/green>=12.5, crosses 12.5, threshold ref-lines at 11.5+12.5. No render-code fix needed. Full 12-scene re-render 55/55 actions ok.
- [2026-07-22T23:18:04Z] [2026-07-22T17:20Z] [nutrition-demo] CONVERGED iter1: concept 5.0 (UP from run003 iter0 4.0), user-artifact 4.0, stop_done. Scenes 1-11 page_text-md5-IDENTICAL to run002 (5.0), carried forward. Scene 12 self-assessed (no Agent-tool sub-agent in orchestrator ctx): the sparkline RESOLVED both prior sub-5 caps against their own pre-registered fix_recommendation (visual_polish 4->5 and claim_reality 4->5 — the prior finding literally asked for "a compact per-visit sparkline in the timeline panel header" to make the full red->green arc visible in one frame; that exact fix shipped). user-artifact trust stays 4 (Beneficiary-N privacy anonymization, unaffected by the curve). Advisory: timing 4.44, video-judge 4.0.
- [2026-07-22T23:18:04Z] [2026-07-22T17:20Z] [nutrition-demo] VIDEO-RENDER DEFECT caught+fixed (keep-if-better, RENDER class): scene-12 explainer action_mark was a click on the Beneficiary-1 Timeline button — a row deep in the 4977px-tall page — so the connect-ddd-walkthrough render zoomed-to-fit the WHOLE tall page and s12 collapsed to an 18%-width unreadable strip (verified: output content spans only 18% frame width; master footage was full-width 1600px throughout). FIX: re-map s12 clip segment to the SETTLED panel region (master 125.8-131.1s, drill already open, sparkline prominent) + set action_marks=[] (drop the page-fitting zoom). Re-render -> s12 full-width (100%), curve legible. ENGINE NOTE for canopy: an action-mark whose target sits far down a tall scrollable page can drive a fit-whole-page zoom; render should clamp zoom-out or prefer the post-action settled viewport.
- [2026-07-22T23:18:04Z] [2026-07-22T17:20Z] [nutrition-demo] NARRATED HERO produced: reused run002 tight s1-s11 VO verbatim + authored s12 closing-payoff line ("It closes on one child - Beneficiary 1 MUAC climbing across eight follow-ups, out of red, through yellow, into green. Recovered.") + child-landing outro. 110.5s, dead-air 4.5s all under 3.0s threshold, held-frame overrun +21.7s (VO-over-informative-payoff-frame, incl the deliberate recovered-green s12 hold; not silent dead-air). Continuous master recorded WITHOUT de-dwell/skip flags (skip-flags give too-short beats). ElevenLabs key lives at ~/.claude/canopy/.env (NOT canopy/.env).
- [2026-07-22T23:18:04Z] [2026-07-22T17:20Z] [nutrition-demo] EXTERNAL-PUBLISHED via ddd-upload --release-approved (human approved external publishing in-session): external_release gate created+resolved+audited, phase->uploaded. /ddd package assembled (hero_video de43739d + docs 2ffb740a + deck c6c9f2c4 + clip c07a209f) under narrative v3 (ff96b68a). Package: /canopy/w/connect/ddd/nutrition-demo/nutrition-demo-2026-07-22-003.
- [2026-07-22T23:18:04Z] [2026-07-22T17:20Z] [nutrition-demo] CLEAN RELEASE PAGE mechanism: /canopy/ddd-release/<slug>/<run_id> (SPA) reads /api/ddd/release/<run_id>/?t=<token>. Publishing a run whose narrative visibility is already public auto-sets is_public=true + mints a release share_token — NO manual visibility flip needed. Anonymous access: WITH ?t=token -> HTTP 200 (video/docs each carry their own stream token in content_url); WITHOUT token -> 404. Share URL built by FT(e): requires is_public && share_token. Confirmed scene-12 recovery curve renders for an anonymous viewer (byte-identical 110.5s hero streamed via the public token). Release token: sl5hVRMl16ePd_-wHXFFrvVhzYqr80TB.

## nutrition-demo 2026-07-22-004 (13-scene narrated re-render)
- Narrative rewrite (added scene 0 role:overview + per-scene `narrative` VOs) versioned cleanly to v4 via `narrative sync` (content-hash changed → posted); no re-gate needed (user-approved). run bound to v4 review d2fa9a4a.
- 13-scene screenshot render: 57/57 actions ok; scene 11 (AI-caught audit, 2 Hyperzoomed + 3 hidden Not-Hyperzoomed) did NOT hit the text-selector-hidden-first timeout — the recipe already scopes with exact selectors. Converged concept 5.0 / user 4.0 (trust 4 = Beneficiary-N anonymisation, stands).
- DEEP-ROW ZOOM DEFECT RECURRED on the narrated hero's scene 13 (SAM Follow-up Timeline drill): action_mark click on the Timeline button (far down a tall page) → ~18% strip. Fix per memory worked: remap s13 explainer segment to the SETTLED master window (start 134.0s dur 2.8s — panel open, scrolled to follow-ups) + action_marks:[]. Re-render → full 100% width, curve legible red→green. keep-if-better.
- external_release via `--release-approved` (human approved in-session). Public release page auto-minted token (narrative was already public). Anon verify: release API 200 w/ token, 404 without; hero streams 206 video/mp4, size byte-matches local hero.

## nutrition-demo 2026-07-23 (photo rail + AI census; 3 re-records)
- REQUEST-FIDELITY, not concept-fidelity, was the miss that cost a whole render cycle. The v1 per-child view shipped a TEXT visit list when the request (thread msg 8) was explicitly "photographs for each visit on the left rail ... one image per visit that you can expand". No DDD judge catches this class: concept/visual judges score a scene against the spec's own `concept_claim`, and the spec faithfully described the list it built. Only `agent-turn-review` §A (re-read the ORIGINAL request) catches spec-vs-request drift — and it runs at send time, i.e. one render too late. If a narrative is built to satisfy a written request, diff the spec against that request BEFORE the first render.
- `must_succeed: false` turns a broken action into a GREEN run report. The closing scene dismissed the photo overlay via `click css:.fixed.inset-0`; Playwright clicks an element's CENTRE, which is the inner card whose onClick calls stopPropagation — so the click hit the photo and the overlay never closed, covering the payoff chart for the scene's tail. The recorder still printed "70 actions: all ok". Any action whose failure would change what a viewer sees must be `must_succeed: true`. Backdrop-dismiss is never a reliable recorder target — give the component a real close control and target that.
- A modal with click-outside as its ONLY dismissal is a product defect the walkthrough surfaces first. Added a close button + Escape handler; the recorder's need for a deterministic target and the user's need for an obvious exit are the same requirement.
- `ddd-ace-render` does NOT judge. It records → emits spec → renders → uploads. Two cuts shipped unjudged before the human caught the overlay in seconds. Run `ddd-timing-eval` (free, auto-written to the run dir as verdict-timing.json) and `ddd-video-judge` before publishing, or use the auto loop.
- Every `upload-video` MINTS A NEW walkthrough id, so a link already emailed is frozen on the old cut and silently goes stale. Publishing a correction required a second email. For anything shared with humans, hand out the narrative page (/canopy/ddd/<slug>), not the per-walkthrough URL.
- Harness Tailwind ships utilities but NOT `grid-cols-12` / `col-span-*` — a 3-column layout silently stacked. Use explicit grid styles in workflow render_code.
- Ranking demo subjects by NET metric gain surfaces noise (a child bouncing 14.5→13→12→15.4 has a big delta and reads as broken data). Penalise week-over-week drops so the default subject is a monotonic climb.

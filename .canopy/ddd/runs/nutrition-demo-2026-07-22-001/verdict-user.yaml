schema_version: 1
kind: user_artifact
gate: gating
live_state_verified: true
calibration: provisional
rubric_name: user-artifact
ran_at: '2026-07-22T12:57:12Z'
run_dir: /Users/jjackson/emdash/worktrees/connect-labs/nutrition-demo-env/.canopy/ddd/runs/nutrition-demo-2026-07-22-001/

audience:
  name: program manager (nutrition oversight)
  decision: which network manager needs my attention this week, and can I verify the rollup against primary records myself

dimensions:
  task_completion:
    score: 4
    weight: 0.40
    justification: >
      The target task — read the grid, decide which network manager needs attention,
      and verify the flag against primary records — is completable first try without
      help. The aggregate calls SOP MET vs BELOW; cells open inline panels naming the
      flagged worker and linking audit + task; drills reach the real photos and coaching
      transcript. One minor friction: on the drilled weekly-review table the state-aware
      View Task button is horizontally clipped at the viewport (reads 'View Ta'), so a
      user has to know to scroll right to click it, though View Audit is fully reachable.
    fix_recommendation: >
      Frame or keep the flagged row's Actions column in view (scene scroll, or a sticky/
      compact Actions column in the product) so both View Audit and View Task are
      clickable without a horizontal scroll.
    fix_kind: options
  clarity:
    score: 4
    weight: 0.35
    justification: >
      Labels self-explain for a non-technical program manager: SOP MET/BELOW, Clean run
      / All resolved / N open, Flagged/Audits/Tasks, closed-satisfactory vs investigating
      vs closed-suspended, Pass/Fail, Hyperzoomed. Two small things keep it off 5: the
      flag semantics (a suspiciously LOW MAM/SAM rate being the flag) take a moment of
      thought until the coaching transcript explains it, and the top-right program picker
      reads 'KMC (Synthetic)' which is an honest synthetic-env marker but does not match
      the nutrition framing.
    fix_recommendation: >
      Consider a one-line 'why flagged' affordance (an info bubble on the flag pill
      explaining that an implausibly low screening rate triggers review) so the flag
      logic is legible without reading the transcript.
    fix_kind: options
  trust:
    score: 4
    weight: 0.25
    justification: >
      High trust: numbers are internally consistent, dates give recency, the evidence is
      directly inspectable (real MUAC tape photos, coherent AI coaching transcripts, per-
      photo pass/fail), and the synthetic marker is disclosed at the top level (exempt).
      Entity-level data looks real (real names, plausible MUAC values, real field photos).
      One provenance signal is slightly off: on Vida's audit the uniform 'Hyperzoomed' AI
      badge does not match the visible content of ~3 of the 5 photos (glare / obstruction
      / mispositioning rather than hyperzoom), which a sharp reader inspecting the images
      would notice.
    fix_recommendation: >
      Make each AI photo badge match its image (2-photo pure-hyperzoom corpus, per-photo
      true defect labels, or softer 'framing-flagged' wording) so the AI verdict is
      verifiable against the images it labels.
    fix_kind: options

overall_score: 4
overall_rule: lowest
verdict: pass
blocking_reason: null
